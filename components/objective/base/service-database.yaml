apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: objective-database
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: objective-database
  name: objective-database
spec:
  ports:
    - port: 
      protocol: TCP
      targetPort: 
  selector:
    app.kubernetes.io/instance: objective-database
